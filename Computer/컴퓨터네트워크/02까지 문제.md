

1. www.naver.com 을 요청했을 때 URL은 어떻게 동작하는가?(만약, 집에서 인터넷으로 이용했을 때!)
   ​

   ​

2. TCP/IP 모델의 구조를 설명해주세요.

   ​

3. Address Resolution protocol 과 Reverse Resolution protocol 그리고 ICMP(Internet Control Message Protocol)은 어떤 역할을 하는가?
   ​

   **IP 주소와 MAC 주소는 프로토콜의 동작 특성상 추가로 고려할 사항이 있다. 예를 들어, 계층 2 프로토콜을 이용해 데이터를 전송하려면 목적지 호스트의 MAC 주소가 필요합니다. 자신의 IP 주소와 MAC 주소는 쉽게 얻을 수 있지만 수신자의 주소는 다르게 처리됩니다. 상대방의 IP 주소는 응용 프로그램 사용자로부터 입력되지만, 상대방 MAC 주소 정보는 어디서도 얻을 수 없다. 따라서 사용자로부터 입력된 IP 주소를 이용해MAC 주소를 구하는 기능이 필요한데, 이것을 ARP(Address Resolution Protocol)가 담당합니다.**

   **일반적으로 호스트의 IP 주소는 하드 디스크의 특정 위치에 보관한다. 하드 디스크가 없는 시스템은 LAN 카드에 내장된 자신의 MAC 주소는 알지만, 자신의 IP 주소는 알 수 없습니다. 그러므로 MAC 주소를 IP 주소로 변환하는 RARP(Reverse Address Resolution Protocol)가 필요합니다.**

   ​

4. IPv6에서 기존 IPv4에 비해 개선된 점을 3가지 이상 나열하고 각각 설명하시오.
   ​

   **IPv6은 또한 방대한 주소 규모 이외에 여러 가지 장점을 제공한다.** 

   **1) 우선 필요한 서비스에 따라 네트워크 대역폭을 확보할 수 있어 인터넷 품질이 향상될 수 있다.** 

   **2) IP 주소에서 여러 네트워크 정보를 담고 있는 부분인 ‘패킷 헤더(packet header)’의 크기는 증가됐지만(IPv4: 20바이트 / IPv6: 40바이트), 구조와 체계가 단순해져 데이터 처리 효율을 높일 수도 있다.** 

   **뿐만 아니라 3) 기존 IPv4 체계보다 보안 기능이 강화되어 보다 안전한 인터넷 사용이 가능다. 보안 통신을 위해 사용되는 IPSec 프로토콜이 IPv6 체계에는 기본 내장되기 때문이다.** 

   **4) 여기에 데이터의 무결성과 보안성을 보장하는 기능도 가미된다.** 

   **아울러 6) IPv6는 IP 주소를 할당하는데 있어 유니캐스트(1:1 통신), 멀티캐스트(1:多 통신), 애니캐스트(1:多 중 1) 방식을 사용하기 때문에 기존처럼 IP 주소가 쓸데 없이 낭비되지도 않는다.**

   ​

5. 흐름제어를 데이터링크층에서도 전송 계층에서도 하는데 이 둘의 차이는 무엇인가?

   **전송 계층에서의 흐름제어는 End to End 로 종단간이다. 이는 즉 데이터 전송이 처음 시작되는 소스(source)호스트 컴퓨터에서 최종 목적기 호스트까지를 의미힌다. / 데이터 링크계층의 경우에는 종단간이 아닌, 이웃과 이웃노드간의 오류제어를 말한다.**

   ​

6. 게이트웨이란? 그 종류는?

   게이트 웨이란? 인터네트워킹 기능을 수행하는 시스템을 일반적으로 지칭한다.
   전송계층이상 상위 계층에서 프로토콜 변환 기능 수행하는 것들.

   종류로 라우터, 브리지. 
   ​

7.  다음 보기는 7개의OSI 계층 중 어느 계층과 관계가 있는지 기술하시오.

   a. 전송 경로의 결정  - **데이터링크**

   b. 흐름 제어 - 데이터링크, **전송계층**

   c. 종단 사용자를 위한 네트워크 접근 - **응용계층**

   d. 한 코드에서 다른 코드로의 데이터 형식을 변환. - **표현계층**

   e. 전체 메세지에 대한 신뢰할 수 있는 종단-대-종단 전송을 제공. - **전송계층**

   f. 프레임 구성. - **데이터 링크 계층**

   g. 전자 우편과 파일 전송과 같은 사용자 서비스 제공 - **응용계층**

   h. 전송 매체를 통해 신호 전송 - **물리계층**

   i. 오류 교정과 재전송 - 데이터링크 / 전송계층

   j. 프레임의 노드-대-노드 전달 책임 - **데이터링크**?

   k. 로그인과 로그아웃 확인 제공 - **세션계층**?

   l. 데이터 표현의 차이로부터 독립성 제공 - **표현 계층**
   ​

8. 네트워크의 데이터 전달 방식에는 Simplex,Half Duplex, Full Duplex 3가지 방식이 있다. 현실 세계에서도다음과 같은 통신 방식이 있는데, 각각 알맞은 전달 방식은?    

   전화 - 풀 듀플렉스

   편지 - 심플

   무전 - 하프


9. 네트워크 7 계층에서, 상위계층과 하위 계층 간의 통신 규칙을 무엇이라고 하는가?

인터페이스같은데... ? 프리미티브?

 

10. OSI 7계층 중, 사용자프로그램으로 구현되는 계층은 몇 계층인가? 또한, 사용자 프로그램 이외의계층은 누가 구현하는가?

567 계층  / 운영체제안에 시스템콜 

11. 다음 그림에서 Logical Address와 Physical Address는 각각 무엇인가? 또한,빈칸에 알맞은 값은 무엇인가? ( 화살표 방향에 유의할 것 )

![](https://ws2.sinaimg.cn/large/006tKfTcgy1fno9r6lpocj30yy0hywg7.jpg)

Logical Address : 4 바이트 (IP 주소)

Physical Address : 6바이트( MAC )

논리적 주소를 물리적 주소로 변환하는 프로토콜??? ARP

네트워크 계층

12. 게이트웨이의 종류 3가지와 그 기능

    ​

    ​

    ​

13. DNS서버가 필요한 이유
    ​

    인터넷을 사용하는 사람이 쉽게 접근가능하게 하기 위해서 필요하다.
    ​
    각 URL을 모두 갖기 힘들기 때문에 분산해서 가져야하기 때문에 그렇다.
    ​

14. IP Address vs MAC Address

    차이점.

    맥어드레스의 경우 하드웨에 부여되는 고유한 주소인 반면, IP어드레스는 .net상에서 고유한 주소이다.

    IP는 라우팅을 위해서, 맥은 이너넷을 활용하기 위해서이다.

15. OSI 7계층 모델의 각 기능

    물리계층 - 종단간 종단을 하드웨어 연결하여 신호를 전송하는 계층  
    데이터링크 - 종단간 종단 사이에 중계 노드를 놓아, 노드사이의 신호를 제어하고 오류를 검출하며 흐름을 제어한다.  
    네트워크계층 - 네트워크 주소를 기반으로 호스트 간 데이터 전송 경로를 결정  
    전송계층 - 종단과 종단사이의 흐름을 제어한다.   
    세션계층 - 상위적(논리적) 연결 개념인 세션 기능 제공  
    표현계층 - 데이터의 의미와 표현 방법을 처리, 통신 양단에서 서로 이해 할 수 있는 표준 방식으로 코딩 / 암호화 압축!!!  
    응용계층 - 사용자에게 유용한 다양한 서비스등을 말한다.  

    ​

16. 네트워크와 네트워크의 연결을 인터네트워킹이라고 한다. 이때 인터네트워킹 기능을 수행하는 시스템을 무엇이라고 하는지 쓰고, 이에 대한 종류를 2가지 이상 쓰시오.  

     인터넷!
```Xml
게이트웨이 - 인터네트워킹을 수행하는 시스템을 일반적으로 지칭하는 말

리피터 - 전송거리가 멀어지면서 약해진 신호 증폭 / 전송 도중에 생긴 잡음까지 같이 증폭되므로 물리적 한계 존재.

브리지 - 논리적으로 분리된 세그먼트사이에서 같은 프로토콜을 이용하는 경우 동일 세그먼트에 속한 것인지 아닌지를 구분하여 처리. 

라우터 - 물리계층, 경로선택기능.
```

​     

17. IP 프로토콜을 지원하는 전 세계의 모든네트워크를 연결된 시스템을 뭐라고 하는가?

    인터넷?

    ​

18.  네트워크 부하가 증가하면 특정 지역에 혼잡이 발생할 수 있는데, 이때 OPI 7계층 모델의 각 계층 중, 이를 제어하는 계층이 무엇인지 쓰고 이유를 쓰시오.  
    **네트워크 계층에서 혼잡제어를 제어합니다.  ECN/ ECE로 혼잡을 제어함**

    혼잡제어를 하기위해서 네트워크 계층 / 전송경로와 관계가 있으므로.
    ​

19. 다음 중 인터네트워킹 장비에 해당하지않는 거은? 4.

1)    브릿지

2)    라우터

3)    게이트웨이

4)    모뎀

 

20. OSI(Open System Interconnection)7계층 중 다음 설명에 해당하는 계층은? **3** 

`통신 송수신 양 종점(end-to-end or end-to-user)간에 투명하고 균일한 전송 서비스를 제공해 주는 계층으로 전송 데이터의 다중화 및 중복 데이터 검출, 누락 데이터의 재전송 등 세부 기능을 가진다.`

1. 응용계층
2. 데이터 링크 계층
3. 전송 계층
4. 표현 계층

   ​



21. `서비스 프리미티브`에 대한 설명으로올바른 것을 모두 고르시오
    1. 연결형 서비스는 데이터를 전송하기 전에 연결을 설정해야 하므로 전화 시스템의 통화 절차와 유사한 방식이다. (O)

    2. 비 연결형 서비스는 연결을 설정하고 해제하는 단계가 필요 없다. (O)
    3. 하위 계층이 상위 계층에 제공하는 서비스는 프리미티브 형태로 구현되며,연결형 서비스에서 사용하는 서비스 프리미티브 종류로 CONNECT, DATA, DISCONNECT가 있다. (O)
    4. 서비스 프리미티브 각각에 대하여 Request,Indication, Response, Confirm 이라는 네 가지 기능을 포함하도록 설계해야 한다.(o)
    5. Request와 Indication은 서버에서 클라이언트로 전송되고, Response와 Confirm은 클라이언트에서 서버로 전송된다.(x)



22. `프로토콜`의 정의, 그리고 주요 요소에 대해서 설명하세요.
    ​
    종단간 종단 또는 노드간 노드간에 상호 약속해놓은 규칙을 말한다.
    `프로토콜의 3요소`   
    **구문, 의미, 타이밍**
    ​
23. 네트워크에 대한 설명으로 옳지 않은 것을 모두 고르세요. 1, 2, 5
    1. 인터넷은 서로 통신할 수 있는 둘 또는그 이상의 네트워크이기도 하고, 수 백만 개의 네트워크가 상호 연결된 네트워크이기도 한다.(O)
    2. 인터페이스는 시스템과 전송매체의 연결지점에대한 규칙이자 같은 노드에 위치한 상하위계층 사이의 규칙이고, 프로토콜은 시스템이 데이터 교환할 때 따르는통신 규칙이자 서로 다른 노드에 위치한 동일 계층끼리의 통신 규칙이다.(O)
    3. ISO에서 제안한 네트워크 구조는OSI 7계층이라 한다. 밑의 단부터 나열하면, 물리 계층, 데이터링크 계층, 네트워크 계층,전송계층, 표현계층, 세션계층,응용계층이 존재한다.(X)
    4. 클라이언트는 서비스를 요청하는 시스템이고서버는 특정 서비스를 제공하는 시스템이다. 이를 기반으로 여러 시스템들은 클라이언트나 서버,둘 중 하나로 구분된다. (X)
    5. 네트워크를 구분하기 위해선 식별자가 필요하다.이를 주소라 부른다. 그 예시로 32비트를사용하는 IPv4, 128비트를 사용하는 IPv6가 있다. (O)
24. ![](https://ws1.sinaimg.cn/large/006tKfTcgy1fnoa8r5udfj30tg0bcjt1.jpg)

    Layer 1 - 전송 매체의 물리적 인터페이스에 관한 사항 - 비트
    Layer 2 - 흐름제어, 오류제어 - 프레임( 데이터링크 )
    Layer 3 -  혼잡제어(경로선택) - 패킷(네트워크)
    Layer 4 - 전송계층 - 흐름제어, 오류제어 - 세그먼트
    Layer 5 - 세션계층 - 동기화 - SPDU
    Layer 6  - 표현계층 - 변환, 암호화, 암축 - PPDU
    Layer 7 - 응용계층 - 응용 환경에서 공통 필요 서비스 제공 - APDU



22. **TCP/IP에대한 설명으로 옳지 않은 것을 모두 고르세요**
    1. TCP/IP 모델에서 네트워크 계층은 IP 프로토콜을 사용하고, 전송계층은 TCP나 UDP를 사용한다. 나머지 계층은 사용자 공간으로서 사용자 프로그램이 구현한다.( X )
        **상위 계층은 사용자 프로그래밍이 하위계층은 OS 계층이 담당한다.**
    2. TCP와 UDP의 차이점은 연결의 유무이다. TCP는 연결 수립 없이 데이터를 송신함으로써 단순하고 빠른비연결형 프로토콜이고, UDP는 전송 전에 연결을 함으로써 신뢰성 있는 안정적인 연결형 프로토콜이다. ( X )
    3. TCP/IP 모델에서 TCP,IP, UDP말고도 추가적으로 필요한 프로토콜은 ARP, RARP, ICMP이다.ARP는 LAN카드에 내정된 MAC 주소를IP주소로 변환하는 프로토콜이고, RARP는 ARP와 반대로, IP주소를 이용해 MAC주소를 구하는 프로토콜이다. ( X )
    4. ICMP는 IP가 동작하는 과정에서 전송 오류 발생을 송신자에게 통지하는 프로토콜이다. ( O )